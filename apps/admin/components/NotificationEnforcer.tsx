"use client";

/**
 * NotificationEnforcer
 * Desativado para evitar barreiras e atritos na experiência de navegação do usuário.
 * As notificações agora são solicitadas de forma contextual e não-bloqueante.
 */
export function NotificationEnforcer({ userId }: { userId: string | null }) {
  return null;
}
